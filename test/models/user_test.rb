require 'test_helper'

class UserTest < ActiveSupport::TestCase
   test "the truth" do
    assert user.valid?
    assert user.#has wardrobe
    assert user.#has articles
    assert user.#has outfits


   end
end
