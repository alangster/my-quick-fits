<div class="page-header">
	<h1>Welcome, <%= current_user.first_name %>!</h1>
</div>

<div class="row show-row">
	<% if current_wardrobe.articles.count == 0 %>
		<%= link_to "Start Adding Your Clothes", new_article_path, :class => "btn btn-default btn-lg user-options" %>
	<% else %>
	<div class="add-item items col-md-4">
		<%= link_to "Add Item", new_article_path, :class => "btn btn-default btn-lg user-options" %>
	</div>
	<div class="make-outfit items col-md-4">
		<%= link_to "Make Outfit", new_outfit_path, :class => "btn btn-default btn-lg user-options" %>
		<% if flash[:notice] %><h3><%= flash[:notice] %></h3><% end %>
	</div>
	<div class="see-wardrobe items col-md-4">
		<%= link_to "See Wardrobe", wardrobe_path(current_wardrobe), :class => "btn btn-default btn-lg user-options" %>
	</div>
	<% end %>
</div>

<style type="text/css">
  body {
    background-image: url(/fresh_snow.png);
  }
</style>